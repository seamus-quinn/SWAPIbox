import filmsData from '../../data/filmsData';
import StarWarsRepo from './cleaner';

describe('StarWarsRepo', () => {
  let starWarsRepo

  beforeEach(() => {
    starWarsRepo = new StarWarsRepo;
  })

  it('should have properties ')

  describe('filmCleaner', () => {

    it('should return a random object with keys of openingCrawl, title, and releaseYear', () => {
      
    })
  })
})